{
  "name": "cardiometrix-monorepo",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "pnpm --filter cardiometrix dev",
    "dev:web": "pnpm --filter cardiometrix dev",
    "dev:mobile": "pnpm --filter cardiometrix-mobile start",
    "dev:risk": "cd services/risk-service && .venv/bin/python -m uvicorn app.main:app --reload --port 8001",
    "build": "pnpm --filter cardiometrix build",
    "lint": "pnpm --filter cardiometrix lint",
    "test": "pnpm --filter cardiometrix test"
  }
}
